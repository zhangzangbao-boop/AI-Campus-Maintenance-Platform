# 本地非容器全链路验收报告

执行时间：2026-07-18  
执行阶段：L1 本地非容器全链路验收  
安全约束：未读取、未显示、未记录 `.env` 内容；未输出 API Key、数据库密码、JWT、内部服务密钥；未写入 C 盘；未启动 Docker；未清空或重置数据库。

## Git 与环境前置检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `.env` 是否被 Git 忽略 | 通过 | `git check-ignore -v .env` 返回 `.gitignore:6:.env` |
| `.env` 是否存在 | 通过 | 仅检查存在性，未读取或显示内容 |
| 本地提交推送 | 通过 | `de2aba03`、`f029c8c0` 已正常 push 到 `origin/main`，未 force push |
| 必需环境变量 | 通过 | 仅校验变量名是否有值，未打印变量值 |
| Embedding 模型文件 | 通过 | 项目内 `local-models/.../model.onnx` 存在 |
| Embedding tokenizer 文件 | 通过 | 项目内 `local-models/.../tokenizer.json` 存在 |

## 外部依赖与端口检查

| 组件 | 端口/地址 | 检查结果 | 影响 |
| --- | --- | --- | --- |
| MySQL | `127.0.0.1:3306` | 未监听 | 无法执行真实数据库迁移，无法启动依赖 MySQL 的后端服务 |
| Nacos | `127.0.0.1:8848` | 未监听 | 服务发现不可用，Gateway 与各微服务无法完成本地注册/发现 |
| Chroma | `http://127.0.0.1:8000` | 未监听 | RAG 知识问答、历史案例向量检索无法真实验收 |
| Gateway | `127.0.0.1:8080` | 未监听 | 无法经 Gateway 执行业务链路 |
| 前端 | `127.0.0.1:5173` | 未监听 | 未启动前端 |
| user-service | `127.0.0.1:9003` | 未监听 | 未启动 |
| repair-service | `127.0.0.1:9004` | 未监听 | 未启动 |
| ai-service | `127.0.0.1:9002` | 未监听 | 未启动 |
| ops-service | `127.0.0.1:9005/9001` | 未监听 | 未启动 |

项目内未发现可直接启动 Nacos 或 Chroma 的本地发行包；系统 PATH 可发现 MySQL 客户端/服务端程序，但 3306 当前未监听。为避免修改系统设置或写入项目外目录，本次未尝试初始化或启动外部依赖。

## 启动与迁移

| 步骤 | 结果 | 说明 |
| --- | --- | --- |
| 必要且幂等迁移 | 未执行 | MySQL 未监听，未连接数据库；未清空、未重置数据库 |
| 启动 user-service | 未执行 | 前置依赖 MySQL/Nacos 不可用 |
| 启动 repair-service | 未执行 | 前置依赖 MySQL/Nacos 不可用 |
| 启动 ops-service | 未执行 | 前置依赖 MySQL/Nacos 不可用 |
| 启动 ai-service | 未执行 | 前置依赖 Nacos/Chroma 不可用 |
| 启动 gateway | 未执行 | Nacos 不可用 |
| 启动前端 | 未执行 | Gateway/后端未启动，无法进行全链路验收 |

本次没有启动新的 Java、Node 或前端进程，因此无需停止本次启动的服务。

## Gateway 实际链路验收

由于 Gateway 未启动，以下链路未能执行真实请求，状态均为“阻塞：依赖未就绪”：

| 链路 | 状态 | 原因 |
| --- | --- | --- |
| 注册、登录及三角色权限 | 阻塞 | MySQL/Nacos/Gateway 未就绪 |
| 报修、派单、维修图片、完成、学生确认、评价和回访 | 阻塞 | MySQL/Nacos/Gateway 未就绪 |
| 公告、WebSocket 通知、统计、导出和审计 | 阻塞 | MySQL/Nacos/Gateway 未就绪 |
| RAG 知识问答、历史案例推荐 | 阻塞 | Chroma/Nacos/Gateway 未就绪 |
| 真实 DeepSeek 分类、完成总结、情感分析和 AI 解释 | 阻塞 | ai-service/Gateway 未启动；未向 DeepSeek 发起请求 |

## 真实 AI 验收状态

| 检查项 | 结果 |
| --- | --- |
| `.env` 中 AI 必需变量存在性 | 通过，仅校验存在性 |
| 期望模型 | `deepseek-v4-flash` |
| 真实 DeepSeek 请求 | 未执行 |
| provider/model 返回验证 | 未执行 |
| 敏感信息输出 | 未输出 API Key、密码、JWT 或内部密钥 |

## Postman 与性能脚本

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| Postman 集合 | 存在 | `postman/Campus_Maintenance_API.postman_collection.json` |
| Postman 环境 | 存在 | `postman/Campus_Maintenance_Local.postman_environment.json` |
| Newman/Postman CLI | 不可用 | 未安装本地运行器，且未下载新工具 |
| Postman 核心链路执行 | 未执行 | Gateway 未启动且运行器不可用 |
| `scripts/perf-baseline.ps1` | 已执行 | 未提供 BaseUrl/UserId/Password，脚本返回待全链路环境执行且未发请求 |

性能脚本本次输出状态为 `PENDING_FULL_CHAIN_ENVIRONMENT`；请求发送数为 0，因此不记录成功率、平均响应时间或 P95，避免伪造结果。

## 最终状态

| 组件 | 最终状态 |
| --- | --- |
| MySQL | 未监听 `127.0.0.1:3306` |
| Nacos | 未监听 `127.0.0.1:8848` |
| Chroma | 未监听 `127.0.0.1:8000` |
| 本次启动的 Java/前端进程 | 无 |
| 数据库 | 未连接、未迁移、未重置 |

## 结论

L1 全链路验收未通过，原因是本地非容器依赖 MySQL、Nacos、Chroma 均未就绪，且缺少 Postman CLI 运行器。代码层面未发现需要最小修复的问题；本次仅记录真实阻塞状态和可复现检查结果。
