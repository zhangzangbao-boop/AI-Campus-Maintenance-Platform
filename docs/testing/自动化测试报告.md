# 自动化测试报告

执行时间：2026-07-18 16:14-16:16（Asia/Shanghai）  
执行机器：本地开发环境  
约束：Maven 仓库固定为项目内 `local-cache/maven`；未下载模型；未启动生产外部服务；报告不包含 Token、密码或密钥。

## 后端全模块测试

执行命令：

```powershell
cd smart-backend
mvn "-Dmaven.repo.local=D:\WUT-Projects\GraduationInternshipB\AI-Campus-Maintenance-Platform\local-cache\maven" test
```

执行结果：通过。

| 模块 | 测试数 | 失败 | 错误 | 跳过 |
| --- | ---: | ---: | ---: | ---: |
| qiyun-common | 30 | 0 | 0 | 0 |
| qiyun-user-service | 118 | 0 | 0 | 0 |
| qiyun-repair-service | 106 | 0 | 0 | 0 |
| qiyun-ops-service | 101 | 0 | 0 | 0 |
| qiyun-ai-service | 46 | 0 | 0 | 0 |
| qiyun-biz-service | 70 | 0 | 0 | 0 |
| qiyun-gateway | 18 | 0 | 0 | 0 |
| 合计 | 489 | 0 | 0 | 0 |

## 前端构建

执行命令：

```powershell
cd smart-frontend
npm.cmd run build
```

执行结果：通过。

说明：

- 构建输出目标为 `backend/src/main/resources/static`。
- Vite/Rolldown 报告 `index-*.js` chunk 超过 500 kB，这是构建体积提示，不影响本次构建通过。
- 构建生成的静态产物变动已还原，避免把非 F18 内容混入提交。
- `smart-frontend/package.json` 未配置 `test` 脚本，本次未运行前端测试，也未新增大型前端测试工具。

## 本次修复的问题

1. `BackupApiTests` 中路径穿越用例仍期望 5xx；当前业务已正确返回 400，测试断言已修正为 `BadRequest`。
2. `OpsSecurityApiTests` 中知识库搜索/推荐用例仍按公开接口断言 200；当前控制器要求 `STUDENT/STAFF/ADMIN` 登录角色，测试已改为未认证返回 401。
3. `OpsCoreApiTests` 的通知读取只构造 JWT，未准备启用用户引用，导致业务层按“用户不存在或已禁用”拒绝；测试补充启用管理员用户夹具，不放宽业务校验。

## 一键测试脚本

新增 `scripts/run-all-tests.ps1`，用于容器验收或本地重复执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\run-all-tests.ps1
```

脚本行为：

- 使用项目内 `local-cache/maven` 执行 `smart-backend` 全模块 Maven 测试；
- 执行 `smart-frontend` 构建；
- 如前端未来配置 `test` 脚本，则自动运行；当前无 `test` 脚本时明确输出跳过原因。

## 结论

- 后端全模块自动化测试：489 个，通过 489 个，失败 0，错误 0，跳过 0。
- 前端构建：通过。
- 未执行真实性能压测；原因是当前未提供全链路运行环境和测试账号，详见 `docs/testing/性能测试说明.md`。
